# Copy to .env for local development only. Never commit .env.
APP_ENV=development
LOG_LEVEL=info
CANARYNOTES_DB=data/canarynotes.sqlite3
HOST=127.0.0.1
PORT=3000

# Private LAN use only, after confirming the machine firewall/network boundary:
# HOST=0.0.0.0
