# Contributing

Keep changes focused, add or update tests for behavior changes, and run:

```bash
./scripts/check.sh
```

Do not commit secrets, local databases, backups, logs, generated build output, or private note data.
